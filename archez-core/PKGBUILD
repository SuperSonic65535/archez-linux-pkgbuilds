# Maintainer: SuperSonic65535 <SuperSonic65535@proton.me>
pkgname=archez-core
pkgver=$(date +%Y.%-m.%-d)
pkgrel=$(expr $(date +%-S) + $(expr 60 \* $(date +%-M)) + $(expr 3600 \* $(date +%-H)))
pkgdesc='ArchEZ minimal core installation'
arch=(any)
url='https://github.com/SuperSonic65535/archez-linux-pkgbuilds'
replaces=('zestiso-core' 'zestiso-branding')
depends=(
	arch-tweaks
	base
	bashrc-d
	dosfstools
	efibootmgr
	KSMBD-MODULE
	linux-firmware
	sudo
)
