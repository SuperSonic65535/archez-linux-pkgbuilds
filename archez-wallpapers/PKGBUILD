# Maintainer: Dankcuddlybear <dankcuddlybear@protonmail.com>
pkgname=archez-wallpapers
pkgver=$(date +%Y.%-m.%-d)
pkgrel=$(expr $(date +%-S) + $(expr 60 \* $(date +%-M)) + $(expr 3600 \* $(date +%-H)))
pkgdesc='Wallpapers for ArchEZ Linux'
arch=(any)
url='https://github.com/SuperSonic65535/archez-linux-pkgbuilds'
replaces=('zestiso-wallpapers')
package() {
	cp -r "$srcdir/../files/usr" "$pkgdir"
	"$srcdir/../../set-default-permissions.sh" "$pkgdir"
}
