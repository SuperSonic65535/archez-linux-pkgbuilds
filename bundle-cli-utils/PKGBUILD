# Maintainer: SuperSonic65535 <SuperSonic65535@proton.me>
pkgname=bundle-cli-utils
replaces=(linux-utilities-meta)
pkgver=$(date +%Y.%-m.%-d)
pkgrel=$(expr $(date +%-S) + $(expr 60 \* $(date +%-M)) + $(expr 3600 \* $(date +%-H)))
pkgdesc='Useful Linux utilities - all-in-one bundle'
arch=(any)
url='https://github.com/SuperSonic65535/archez-linux-pkgbuilds'
depends=(
	bash-completion
	ddrescue
	dmidecode
	dosfstools
	hdparm
	gptfdisk
	lsscsi
	man-db
	man-pages
	mtools
	polkit
	sudo
	texinfo
	usbutils
)
