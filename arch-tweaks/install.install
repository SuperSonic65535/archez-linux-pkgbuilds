post_upgrade() {
	if [ $(vercmp 2026.7.12-1 $2) -ge 0 ]; then
		if [ -f /usr/bin/grub-mkconfig ]; then
			echo "Applying GRUB bug fixes"
			cp /usr/share/arch-tweaks/custom-configs/etc/grub.d/10_linux /etc/grub.d/10_linux
		fi
	fi
}
